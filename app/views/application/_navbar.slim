nav.navbar.navbar-expand-lg.navbar-light.flex-column.flex-md-row.sticky-top[style="background-color: #e3f2fd;"]
  a.navbar-brand[href=root_url]= render 'logo.svg', style: 'height: 24px; width: 80px'
  .online-status-badge[data-online-status-badge title='unknown status']
  button.navbar-toggler[type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Навигация"]
    span.navbar-toggler-icon
  #navbarSupportedContent.collapse.navbar-collapse
    ul.navbar-nav.mr-auto
      - unless Rails.env.production?
        li.nav-item
          .mt-2.ml-3.mr-3.badge.badge-warning= Rails.env
      = active_link_to title_with_counter(t('.boards'), current_user.boards.count), accounts_url, class: 'nav-link', wrap_tag: :li, wrap_class: 'nav-item'
      li.nav-item
        .mt-2.ml-2= render 'switch_locale'
      /li
        /form.form-inline.my-2.my-lg-0.ml-lg-5
          /.input-group.input-group-sm
            /input.form-control[type="search" placeholder="Искать" aria-label="Search"]
            /.input-group-append
              /button.btn.btn-outline-success.btn-sm.my-2.my-sm-0[type="submit"]
                /| Найти
    ul.navbar-nav.navbar-right
      li.nav-item.dropdown
        = link_to '#', class: 'my-lg-0 py-lg-0 nav-link dropdown-toggle', data: { toggle: 'dropdown' }, id: :navbarDropdown, role: :button, 'aria-haspopup' => true, 'aria-expanded' => false do
          = gravatar_tag current_user, 
            size: 32, default: :monsterid,
            html: { style: 'border-radius: 50%', title: t('helpers.gravatar_hint') }
          span.ml-2
            span= current_user.public_name
        .dropdown-menu.dropdown-menu-right[aria-labelledby="navbarDropdown"]
          = link_to t('.profile'), profile_url, class: 'dropdown-item'
          - if Rails.env.development? || Rails.env.staging?
            = link_to 'Letter Opener', letter_opener_web_url, class: 'dropdown-item'
          .dropdown-divider
          = link_to t('.logout'), sessions_url, method: :delete, class: 'dropdown-item'
