<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
    </style>
  </head>

  <body>
    <p>
      <% if @user.present? %>
        <%= t '.hello_user', name: @user.name %>
      <% else %>
        <%= t '.hello_anonymous' %>
      <% end %>
    </p>
    <hr/>

    <%= yield %>

    <p>
      <%= t '.buy', title: Settings.title %>
    </p>
  </body>
</html>
